import React from "react";

const Username = ({ message, userId }) =>{
 render(
  <strong>
        <strong {message.userId === userId }style={{ color: "blue" }}>{message.userName}</strong>
   
  </strong>
)};

export default Username;
